importante:
    usar en el folder donde esta localizado setup.py el siguiente comand:
    python3 -m build

    leer la pagina guardada de la doc para ver project structure



tareas
    empezar a trabajar sobre los filtros de la aplicacion : done
    y las botoneras de exportacion...

tarea actual... implmentar el funcionamiento del boton de cargar filtros y de guardarlos

todo
elaborar una herramienta que elimine una entrada por su id, insertar el id de la entrada y eliminar
elaborar dentro de la misma herramienta la posibilidad de eliminar por id, para revertir un cambio en especial
    pasos:
    on_delete_by_id_clicked(id):
        confirmar deleting -> return if False else:
        crud_driver(self, diary, delete, {field: id, opertor: = value: (id,)})

